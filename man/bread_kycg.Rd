% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kycg.R
\name{bread_kycg}
\alias{bread_kycg}
\title{Enrich BREAD classifications against KnowYourCG databases}
\usage{
bread_kycg(
  fit,
  which = c("hypermethylated", "hypomethylated"),
  databases = NULL,
  platform = c("EPIC", "EPICv2", "HM450", "MM285"),
  universe = NULL,
  alternative = "greater",
  include_genes = FALSE
)
}
\arguments{
\item{fit}{A \link{BreadFit}.}

\item{which}{Classification level(s) to enrich. Default
\code{c("hypermethylated", "hypomethylated")} runs both as separate queries.
Use a single string to run one.}

\item{databases}{Character vector of KYCG database group titles. Default
\code{NULL} selects defaults by \code{platform}.}

\item{platform}{One of \code{"EPIC"}, \code{"EPICv2"}, \code{"HM450"}, \code{"MM285"}. Default
\code{"EPIC"}. Only used when \code{databases = NULL}.}

\item{universe}{Optional universe of probe IDs (character vector). Default
\code{NULL} uses all probes in the BREAD mapping (i.e. probes covered by
\code{features}), which is the right universe for a region-level enrichment.}

\item{alternative}{\code{"greater"} (default), \code{"two.sided"}, or \code{"less"}.}

\item{include_genes}{Passed through to \code{\link[knowYourCG:testEnrichment]{knowYourCG::testEnrichment()}}.}
}
\value{
A tidy \code{data.frame} with one row per tested set, containing a
\code{query} column naming the classification level (\code{"hypermethylated"} /
\code{"hypomethylated"} / ...) alongside standard KYCG columns (\code{dbname},
\code{estimate}, \code{p.value}, \code{FDR}, ...).
}
\description{
For each region classified as \code{"hypermethylated"} or \code{"hypomethylated"}
(or any level you pick), extracts the constituent EPIC / HM450 / MM285
probes and runs \code{\link[knowYourCG:testEnrichment]{knowYourCG::testEnrichment()}} against one or more CpG
annotation databases. This is how you get from a region-level BREAD
result to \emph{"is the hyper set enriched for Polycomb targets / AP-1 binding
sites / PMDs / ..."}.
}
\section{Databases}{

If \code{databases = NULL}, the function picks a sensible default set for the
platform you specify via \code{platform} (e.g. \code{"EPIC"}, \code{"EPICv2"}, \code{"HM450"},
\code{"MM285"}). Pass a character vector of \code{\link[knowYourCG:listDBGroups]{knowYourCG::listDBGroups()}} titles
to override.
}

\examples{
suppressPackageStartupMessages(library(SummarizedExperiment))

se  <- readRDS(system.file("extdata", "vitc_ag06561.rds", package = "BREAD"))
reg <- readRDS(system.file("extdata", "vitc_regions.rds", package = "BREAD"))
se_ctrl <- se[, se$condition == "ctrl"]

fit <- fit_bread(se_ctrl, reg, ~ passage)
table(results(fit)$classification)

# The enrichment call downloads KnowYourCG reference databases, so it is
# not run here.
\dontrun{
enr <- bread_kycg(fit, which = "hypermethylated",
                  platform = "EPICv2")
head(enr[enr$FDR < 0.01, ])
}

}
