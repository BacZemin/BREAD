% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_region_posterior}
\alias{plot_region_posterior}
\title{Posterior density of the contrast coefficient per region}
\usage{
plot_region_posterior(fit, region_id = NULL, n_grid = 500L, show_delta = TRUE)
}
\arguments{
\item{fit}{A \link{BreadFit}.}

\item{region_id}{Character vector of region IDs. If \code{NULL} (default),
all fitted regions are plotted.}

\item{n_grid}{Number of grid points for the density curve. Default \code{500}.}

\item{show_delta}{Logical. Draw dashed guides at \verb{+/- delta}? Default \code{TRUE}.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Plots the analytical scaled-Student-t posterior of the contrast coefficient
for one or more regions, with vertical guides at \code{0} and \verb{+/- delta} and a
color by final classification. When multiple regions are supplied the plot
facets by region.
}
\examples{
suppressPackageStartupMessages(library(SummarizedExperiment))

se  <- readRDS(system.file("extdata", "vitc_ag06561.rds", package = "BREAD"))
reg <- readRDS(system.file("extdata", "vitc_regions.rds", package = "BREAD"))
se_ctrl <- se[, se$condition == "ctrl"]

fit <- fit_bread(se_ctrl, reg, ~ passage,
                 feature_class_col = "feature_class")

# Pick a region that was actually called hypermethylated; passing
# `region_id = NULL` would facet every region in the fit.
res <- results(fit)
rid <- res$region_id[which(res$classification == "hypermethylated")[1]]
if (is.na(rid)) rid <- res$region_id[1]

plot_region_posterior(fit, region_id = rid)
}
