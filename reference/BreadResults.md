# The `BreadResults` S4 class

Structured result table wrapper. Reserved for downstream reporting
helpers that may want a dedicated class rather than a bare data.frame.

## Slots

- `table`:

  A `data.frame` of per-region results.

- `params`:

  List of classification parameters.
